import {Script} from 'node:vm';
export const PROGRAM_GUIDE=`Write real JavaScript using the island SDK, not a preset scene JSON. No imports, network, DOM, storage or server access. The program is executed in an isolated browser worker. Keep the existing island ecology unless asked otherwise; your program adds a custom layer. Re-submit the complete program to update it.
SDK: island.mesh({id,shape:'box'|'sphere'|'cone'|'cylinder'|'custom',color:'#rrggbb',position:[x,y,z],scale:[x,y,z],rotation:[x,y,z],vertices?:flatXYZArray,indices?:triangleIndices}). Custom meshes let you construct new geometry, waterways, boats, flowers, anything within the island bounds. Coordinates and all transformed vertices must stay inside x,z -10..10,y -7..14. Default primitives have unit dimensions (sphere radius .5). Max 150 meshes, 18000 vertices total. Scale positive <=20. Provide unique IDs. Spheres are smooth. Optional detail:"high" on spheres increases tessellation; optional roughness:0..1, opacity:0..1, emissive:"#rrggbb", smooth:true for custom geometry. Materials are double-sided. Use tasteful pastel greens, warm cream, blue water and restrained blush, preserving the healing handmade look.
For existing animals and furniture prefer island.asset({id,kind,position:[x,y,z],rotation:[0,yaw,0],scale:[1,1,1]}). Available kinds: cat,dog,bird,rabbit,bear,fox,unicorn,sofa,bed,desk,chair,table,plant,lamp. These load the shared storybook animal models and curated furniture GLB assets; keep natural colors, do not recreate them from primitive spheres. Bear is a panda; bird is a chick. Max 32 library assets per program. Their bounds count toward the island boundary. Interior furniture should instead use scenePatch.objects and roomId so they persist and remain interactive.
For animals, design one readable, graceful silhouette before adding detail. Keep the head smaller than the torso, legs slender and long enough to carry the body, joints close together, and colors restrained. Prefer tapered custom geometry for the neck, muzzle, ears, mane, tail and wings. Avoid making an animal from a stack of similarly sized spheres; it reads as a toy pile rather than a living creature.
island.onFrame((seconds)=>{island.move('id',{position:[x,y,z],rotation:[x,y,z],scale:[x,y,z]})}); animation sampled at 10fps. island.onClick(id=>{...}) for mesh clicks; use onFrame to animate the result. No native Three.js reference. Use normal JS functions, arrays, loops, math and state to build new meshes and gameplay. Never endless loops. Max64KB source. No runtime dependencies. Errors stop only this program. Persistence covers source; in-game transient state resets on reload. Do not claim unsupported gameplay or tested behavior without evidence.`;
export function validateProgram(source){if(typeof source!=='string'||!source.trim()||Buffer.byteLength(source)>60000)throw Object.assign(new Error('岛屿代码需为 1–60000 字节'),{status:400});try{new Script(`(function(island){"use strict";\n${source}\n})`);}catch{throw Object.assign(new Error('岛屿代码包含 JavaScript 语法错误'),{status:400});}return source;}
